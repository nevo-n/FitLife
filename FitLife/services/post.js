const Post = require("../models/post");

async function add_post(title, content) {

    const user = new User({
        _id: username,
        password
    });

    await user.save()        
}

module.exports = { add_post }